# This file was generated by upptime/uptime-monitor@v1.24.3
#
# ===============================
# Do not edit this file directly!
# ===============================
#
# Your changes will be overwritten when the template updates (daily)
# Instead, change your .upptimerc.yml configuration: https://upptime.js.org/docs

name: Summary CI
on:
  schedule:
    - cron: "0 0 * * *"
  repository_dispatch:
    types: [summary]
  workflow_dispatch:
jobs:
  release:
    name: Generate README
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.3
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.ROBOT_TOKEN }}
      - name: Update summary in README
        uses: upptime/uptime-monitor@v1.24.3
        with:
          command: "readme"
        env:
          GH_PAT: ${{ secrets.ROBOT_TOKEN }}
      - name: Run readme-repos-list
        uses: koj-co/readme-repos-list@master
        with:
          token: ${{ secrets.ROBOT_TOKEN }}
          query: "topic:upptime"
          size: 20
          max: 1000
          one-per-owner: true
